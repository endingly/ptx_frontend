[metadata]
name = ptx_frontend
version = 0.0.1
description = ptx frontend helper packages
author = endingly

[options]
packages = find:
include_package_data = True

[options.packages.find]
where = .
exclude =
    test
    test.*
    tests
    tests.*