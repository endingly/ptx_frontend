// Auto-generated. Do not edit manually.
//
// This file defines the global union of all generated PTX instruction types.

#pragma once

{% for include in std_includes %}
#include <{{ include }}>
{% endfor %}

{% for include in project_includes %}
#include "{{ include }}"
{% endfor %}

namespace {{ namespace }} {

/**
 * The complete generated PTX instruction type.
 *
 * Operand may be ParsedOp during parsing, or another OperandLike type after
 * identifier resolution / lowering.
 */
template <OperandLike Operand>
using PtxInstruction = std::variant<
{% for instr in instructions %}
    // {{ instr.category }}: {{ instr.opcode }}
    {{ instr.cpp_type }}<Operand>{{ "," if not loop.last else "" }}
{% endfor %}
>;

/**
 * Instruction type directly produced by the PTX parser.
 */
using ParsedInstruction = PtxInstruction<ParsedOp>;

inline constexpr std::size_t kPtxInstructionAlternativeCount =
    {{ instructions | length }};

}  // namespace {{ namespace }}