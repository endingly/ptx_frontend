module;
#include <type_traits>
export module utils;

export namespace ptx_frontend::utils {

template <typename T, typename... Ts>
inline constexpr bool is_one_of = (std::is_same_v<T, Ts> || ...);

};